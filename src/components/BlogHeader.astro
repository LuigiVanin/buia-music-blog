---
interface Props {
    title: string;
    pubDate: string;
    description: string;
    imagePath: string;
    showImage: boolean;
}

const { title, description, showImage, imagePath, pubDate } = Astro.props;
---
